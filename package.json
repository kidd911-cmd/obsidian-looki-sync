{
  "name": "obsidian-looki-sync",
  "version": "1.0.4",
  "description": "Sync Looki Moments and For You into daily memory notes, with optional image/video sync.",
  "main": "main.js",
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production",
    "version": "node version-bump.mjs"
  },
  "keywords": [
    "obsidian",
    "looki",
    "sync"
  ],
  "author": "Jeremy Jin",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.0.0",
    "esbuild": "^0.23.0",
    "obsidian": "^1.7.0",
    "tslib": "^2.6.0",
    "typescript": "^5.4.0"
  }
}
